import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Type errors and lint problems are caught by `npm test` and the type-check before every
  // deploy, so the build itself stays strict too: neither is ignored here on purpose.
};

export default nextConfig;
